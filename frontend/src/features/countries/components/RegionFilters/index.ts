/* ==========================================================================
   RegionFilters — Barrel Export
   ========================================================================== */

export { RegionFilters, REGION_OPTIONS, ETHNIC_GROUP_OPTIONS } from './RegionFilters';
export type {
  RegionFiltersProps,
  RegionOption,
  EthnicGroupOption,
} from './RegionFilters';
